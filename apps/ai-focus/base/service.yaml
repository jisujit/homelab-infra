apiVersion: v1
kind: Service
metadata:
  name: ai-focus-service
  labels:
    app: ai-focus
spec:
  selector:
    app: ai-focus
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
  type: NodePort
